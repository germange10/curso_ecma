let hello = "hello";
let world = "word";
let epicPhrase = hello + ` ` + world + `!`
console.log(epicPhrase);

//template literals

let hello = "hello";
let world = "word";

let epicPhrase2 = `${hello}      ${world}! el espaciado es importante`;
console.log(epicPhrase2) 





//multi-line strings
let lorem = `esta es una frase epica
la continuacion de esa frase epica`;
console.log (lorem)

let lorem2 = `per aspera 
ad astra`;
console.log (lorem2)